const p=new URLSearchParams(location.search),b=PX_CURRICULUM.findBranch(p.get('branch'));const root=document.querySelector('#subjectPage');
if(!b){root.innerHTML='<div class="empty">Branch not found. <a href="./">Return to catalogue</a>.</div>'}else{root.innerHTML=`<div class="eyebrow">${b.short} · ADMISSION SESSION 2026 · SEMESTER I</div><h1 style="font:800 clamp(42px,6vw,76px) Manrope;letter-spacing:-.055em;line-height:1.05;margin:0;max-width:900px">${b.name}</h1><p style="color:var(--muted);font-size:18px;max-width:720px">Select a subject to open its unit map. Every course is kept under its own branch to prevent curriculum mixing.</p><div class="grid" style="margin-top:42px">${b.subjects.map((s,i)=>`<a class="card" href="topic.html?branch=${b.id}&subject=${encodeURIComponent(s[0])}"><div><span class="code">${String(i+1).padStart(2,'0')} · ${esc(s[0])}</span><h3>${esc(s[1])}</h3><p>${s[2].length} units · curriculum map</p></div><span class="arrow">→</span></a>`).join('')}</div><div style="margin-top:50px;padding:24px;border:1px solid var(--line);border-radius:18px;background:var(--paper)"><b>Source note</b><p class="muted" style="margin:7px 0 0">Course mapping follows the supplied SBTE Bihar Admission Session 2026 Semester-I curriculum documents. Detailed lesson text is authored separately rather than copied from textbooks.</p></div>`}
function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}