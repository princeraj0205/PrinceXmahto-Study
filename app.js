const $=s=>document.querySelector(s);const esc=s=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
function branchCard(b){return `<a class="card" href="subject.html?branch=${encodeURIComponent(b.id)}"><div><span class="code">${b.short} · SEMESTER I</span><h3>${esc(b.name)}</h3><p>${b.subjects.length} curriculum subjects · Admission Session 2026</p></div><span class="arrow">↗</span></a>`}
function render(){const box=$('#branches');if(!box)return;const q=($('#search')?.value||'').trim().toLowerCase();const filtered=PX_CURRICULUM.branches.filter(b=>!q||b.name.toLowerCase().includes(q)||b.short.toLowerCase().includes(q)||b.subjects.some(s=>s.join(' ').toLowerCase().includes(q)));box.innerHTML=filtered.length?filtered.map(branchCard).join(''):`<div class="empty">No matching branch or subject. Try a course code such as <b>2600101</b>.</div>`}
$('#search')?.addEventListener('input',render);$('#printHome')?.addEventListener('click',()=>window.print());render();