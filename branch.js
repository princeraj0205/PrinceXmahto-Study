const q=new URLSearchParams(location.search),b=PX_CURRICULUM.findBranch(q.get('branch')),root=document.querySelector('#branchRoot');const esc=s=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));if(!b)root.innerHTML='<div class="empty">Branch not found. <a href="./">Return to catalogue</a>.</div>';else{const topicCount=b.subjects.reduce((n,s)=>n+s[2].length,0);root.innerHTML=`<div class="eyebrow">${esc(b.short)} · ADMISSION SESSION 2026 · SEMESTER I</div><h1 class="page-title">${esc(b.name)}</h1><p class="lead">Select a subject to continue to units and topic-level study materials.</p><div class="subject-meta"><span>${b.subjects.length} subjects</span><span>${topicCount} mapped units/topics</span><span>Semester I</span></div><div class="topic-grid">${b.subjects.map(s=>`<a class="topic-card" href="subject.html?branch=${b.id}&subject=${encodeURIComponent(s[0])}"><span>${esc(s[0])}</span><h2>${esc(s[1])}</h2><p>${s[2].length} units · Open subject →</p></a>`).join('')}</div><div class="source-strip">This branch catalogue is kept separate from other programmes. Course mapping comes from the supplied SBTE Bihar curriculum document.</div>`}